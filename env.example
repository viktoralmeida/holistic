# Database Configuration
# MongoDB connection string - replace with your actual MongoDB URI
DATABASE_URI=mongodb://localhost:27017/holisticpoint

# Payload CMS Configuration
# Generate a secure secret key for Payload CMS
PAYLOAD_SECRET=your-super-secret-payload-key-here-change-this-in-production

# Application URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
FRONTEND_URL=http://localhost:3000
ADMIN_URL=http://localhost:3000

# Stripe Configuration
# Get your keys from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Resend API Configuration
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=your_resend_api_key_here

# Email Configuration
# Set your verified domain email address
# Format: "Your Name <email@yourdomain.com>" or just "email@yourdomain.com"
RESEND_FROM_EMAIL=Holistic Point <info@holisticpoint.pl>

# Admin Email Configuration
# Email address where contact form submissions and payment notifications will be sent
ADMIN_EMAIL=admin@holisticpoint.pl

# Super Admin Configuration
# These credentials will be used when creating the super admin user
# Run "npm run setup:admin" to create super admin with these credentials
SUPER_ADMIN_EMAIL=admin@holisticpoint.pl
SUPER_ADMIN_USERNAME=admin
SUPER_ADMIN_PASSWORD=admin123

# Free Storage Configuration
# Files are stored as Base64 in the database - no additional configuration needed!

# Development/Production Environment
NODE_ENV=development

# Setup Instructions:
# 1. Copy this file to .env.local
# 2. Replace all placeholder values with your actual credentials
# 3. For MongoDB: Set up a MongoDB database (local or cloud like MongoDB Atlas)
# 4. For Stripe: Get your keys from https://dashboard.stripe.com/apikeys
# 5. For Resend: Sign up at https://resend.com and get your API key
# 6. For Storage: Files are stored as Base64 in MongoDB (completely free!)
# 7. Generate a secure PAYLOAD_SECRET (use a password generator)
# 8. Update all URLs for production deployment
